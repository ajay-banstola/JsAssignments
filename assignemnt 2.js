"use strict";

const radius = 30;

const circum = (radius) => { return 2*Math.PI*radius}
const area = (radius) => {return Math.PI*radius*radius}
//Ajay banstola
console.log("The circumference of circle is " + circum(radius));
console.log("The area of the cicle is " + area(radius));